# CocoaPods — `pod install` regenerates all of these from the committed Podfile + .xcodeproj.
Pods/
*.xcworkspace
Podfile.lock

# Xcode build output / user state
build/
DerivedData/
*.xcuserstate
xcuserdata/

# Never commit a real embed secret (see Shared/Config.swift)
*.local.swift
