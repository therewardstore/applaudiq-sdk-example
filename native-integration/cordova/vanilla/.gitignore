# build output (regenerated by `npm run build` + `cordova prepare`)
www/
dist/

# cordova native platforms + fetched plugins (restored from config.xml + package.json "cordova" field)
platforms/
plugins/

# deps
node_modules/
package-lock.json

# misc
.DS_Store
*.log
