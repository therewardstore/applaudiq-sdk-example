// Bare React Native uses react-native-linear-gradient. (The Expo example re-exports
// `expo-linear-gradient` here instead — same `LinearGradient` API: colors / start / end.)
export { default as LinearGradient } from 'react-native-linear-gradient';
