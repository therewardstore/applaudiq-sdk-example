// Expo bundles a zero-config gradient. (The React Native CLI example re-exports
// `react-native-linear-gradient` here instead — same `LinearGradient` API.)
export { LinearGradient } from 'expo-linear-gradient';
