<?xml version="1.0" encoding="utf-8"?>
<!-- DEV ONLY: allow cleartext to a LOCAL portal so the example can run against the local stack
     from an emulator (10.0.2.2 is the host loopback). Production uses HTTPS only; remove this file
     (and the manifest reference) for a release build. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
